# Hostinger SSH Deployment Configuration for gltfvfx.fun
# Copy this file to .env and fill in your actual credentials

# SSH Configuration
# You can find these in your Hostinger control panel under SSH Access
SSH_HOST=gltfvfx.fun
SSH_PORT=65002
SSH_USER=your_hostinger_username

# Remote directory where files should be uploaded
# Usually this is public_html for the main domain
REMOTE_DIR=public_html

# Authentication Method - Use ONE of the following:

# Option 1: SSH Password (less secure, but easier)
SSH_PASS=your_ssh_password

# Option 2: SSH Key Path (more secure, recommended)
# Uncomment the line below and comment out SSH_PASS if using SSH keys
# SSH_KEY_PATH=~/.ssh/id_rsa

# Notes:
# - Get your SSH credentials from Hostinger's control panel
# - Default SSH port for Hostinger is usually 65002
# - If using SSH keys, you'll need to add your public key to Hostinger first
# - Never commit the .env file to version control (it's in .gitignore)
